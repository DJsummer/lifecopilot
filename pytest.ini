[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short

markers =
    unit: 纯逻辑单元测试，不依赖 DB/外部服务
    integration: 集成测试，使用内存 DB
    e2e: 端到端 Selenium 测试，需要运行中的浏览器
    auth: 认证相关测试
    member: 成员管理相关测试
    health: 健康数据相关测试

